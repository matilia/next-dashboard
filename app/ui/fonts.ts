import {Lusitana} from "next/font/google";
import {Inter} from "next/font/google";

export const lutanist = Lusitana({weight: '400', subsets: ['latin']});
export const inter = Inter({ subsets: ['latin']});
